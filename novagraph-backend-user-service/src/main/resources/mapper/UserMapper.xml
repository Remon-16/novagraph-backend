<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tech.novagraphbackenduserservice.infrastructure.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.tech.novagraphbackendmodel.user.entity.User">
            <id property="id" column="id" />
            <result property="userAccount" column="userAccount" />
            <result property="userPassword" column="userPassword" />
            <result property="userName" column="userName" />
            <result property="userAvatar" column="userAvatar" />
            <result property="userProfile" column="userProfile" />
            <result property="userRole" column="userRole" />
            <result property="roomCode" column="roomCode" />
            <result property="editTime" column="editTime" />
            <result property="createTime" column="createTime" />
            <result property="updateTime" column="updateTime" />
            <result property="isDelete" column="isDelete" />
    </resultMap>

    <sql id="Base_Column_List">
        id,userAccount,userPassword,userName,userAvatar,userProfile,
        userRole,roomCode,editTime,createTime,updateTime,
        isDelete
    </sql>

    <select id="selectUserWithStatsById" resultType="com.tech.novagraphbackendmodel.user.entity.UserWithStats">
        SELECT
        u.id,
        u.userAccount,
        u.userName,
        u.userAvatar,
        u.userProfile,
        u.userRole,
        u.roomCode,
        u.editTime,
        u.createTime,
        u.updateTime,
        ut.userScore,
        ut.followingCount,
        ut.followerCount
        FROM
        user u
        LEFT JOIN
        user_statistics ut ON u.id = ut.userId
        <where>
            u.isDelete = 0
            AND u.id = #{userId}
        </where>
    </select>
</mapper>
